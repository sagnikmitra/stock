# End-to-End Blueprint for Your Daily Custom Screener

## What this system actually needs to be

The biggest mistake would be to build a thin wrapper around the public pages on entity["company","Chartink","stock screener platform"] and call it a product. That would be brittle and half-baked. Your own prompt file already points in the opposite direction: the app is supposed to be a serious internal rule engine, digest system, strategy explainer, screener intersection engine, and backtesting-ready knowledge base, not a tab launcher and not a scraping toy. Your current repo is also not greenfield anymore; the audit says the database schema, strategy engine, worker pipelines, and baseline web/admin routes already exist, so the job now is hardening, source-of-truth cleanup, and finishing the missing contracts, not starting over. fileciteturn0file0 fileciteturn0file9

A few of your current rules need to be corrected before you automate anything. First, “SGX Nifty” should be treated as a legacy label only. The contract has already moved to entity["organization","NSE International Exchange","gift city exchange"] and been renamed Gift Nifty; the strategic routing from entity["organization","Singapore Exchange","singapore exchange"] into NSE IFSC is official, and NSE now surfaces GiftNifty futures directly on its site. Second, “FII holding increased/decreased” is not a same-day pre-market signal; exchange compliance for shareholding pattern is quarterly, while the daily thing you can actually use before/after each session is provisional FII/FPI and DII trading activity. Third, your 4-hour SuperTrend and 9/15 EMA logic cannot run from end-of-day bhavcopy alone; it needs intraday candles. citeturn11view0turn11view1turn12view1turn11view8turn11view7turn11view4turn11view5

That means the right architecture is this: external sites stay as references and parity checks, while your own engine stores canonical strategy versions, computes indicators centrally, runs daily and month-end jobs, explains exactly why a stock matched, and keeps investing rules, swing rules, and capital-allocation rules separate. Your notes say the same thing in plain English: do not mix investing and swing logic blindly, and keep every strategy separate. fileciteturn0file0 fileciteturn0file8

## How your links should map into internal screeners

Most of your externally linked screens already have the correct internal destination in the source matrices and screener registry. The monthly Bollinger setup maps to `investment_bb_internal` and the canonical monthly BB breakout strategy; the trend-continuation link maps to `trend_continuation_internal`; the 4-hour SuperTrend plus 9/15 EMA link maps to `ema_9_15_supertrend_4h_internal`; the 13/34 link maps to `sma_13_34_internal`; the 44 MA link maps to `sma_44_internal`; the RSI-above-80 page maps to `rsi_above_80_internal`; the NSE 52-week-high page maps to `nse_52_week_high_internal`; and the top-gainers plus volume-shockers workflow maps to `btst_top_gainers_volume_shockers_internal`. Your repo even already defines bundle presets for Month End Investment, Swing Daily Check, Breakout Radar, BTST Radar, and Strong Confluence Set. fileciteturn0file11 fileciteturn0file18

The strategy rules behind those mappings are clear enough to implement internally without guessing. Session 6’s BB investment setup is a monthly strategy with price and liquidity filters, monthly RSI strength, and a monthly upper-Bollinger-band breakout, with entry at 1% above the trigger rather than on the raw signal. Session 7 defines Buying in the Dips as monthly RSI at or above 60 plus a daily pullback and a daily resumption entry, and Cross Strategy as lower-Bollinger interaction plus trendline break plus VWAP cross plus green daily candle. Session 8 defines ABC as 50 SMA, lower Bollinger interaction, and a green candle, again with a +1% confirmation and stop below the recent swing low. Session 9 defines breakout quality as a big green candle with a body around 70% or more of range, volume higher than the previous day, meaningful delivery participation, and a close at least 1% above resistance; it also defines Trend Continuation, 13/34 above 200, 44 SMA reclaim, and 9/15 EMA plus SuperTrend on 4-hour candles. fileciteturn0file4 fileciteturn0file5 fileciteturn0file6 fileciteturn0file7 fileciteturn0file8 fileciteturn0file12

Two important screeners are still missing as first-class internal rules, and you should add them. One is your sideways support-reversal setup: support-area reversal, delivery percentage above 45%, volume above volume moving average, close above 5-day EMA, and entry at support plus 15% of the support-resistance band. The other is a true 20-day channel breakout screener: the public Chartink page literally describes it as stocks that break out the 20-day channel, and your own notes say that if a breakout happens, the candle body, volume, delivery, and 1%-above-resistance filters must kick in. Those are not the same trade, so do not merge them. The 15%-above-support rule is an early support-reversal entry; the 1%-above-resistance rule is a confirmed breakout entry. If you mix those two, you will generate garbage signals. fileciteturn0file8 citeturn14search0

There is one more nuance you should preserve in your DSL: ambiguous rules need explicit versioning instead of silent normalization. Your codebase already does this for BB monthly with a raw-note version and a normalized active version, and for Buying in the Dips with a shorthand RSI variant and a canonical variant. That pattern should extend to your breakout delivery threshold, because the notes and summaries vary between 35% and 45%; the sane move is to store a “relaxed canonical” and a “strict” variant, not pretend the ambiguity doesn’t exist. fileciteturn0file12 fileciteturn0file17 fileciteturn0file7 fileciteturn0file8

## Which data sources should be the source of truth

For daily Indian cash-equity screening, the source-of-truth stack should be led by the entity["organization","National Stock Exchange of India","indian stock exchange"], not by media pages. NSE’s historical and live report surfaces give you equity bhavcopy, security-wise price-volume archives, security-wise deliverable-position data, FII/FPI and DII activity, and the live 52-week-high list, with CSV downloads exposed in multiple places. NSE also explicitly defines the deliverable-position ratio, which is exactly the metric your notes call “delivery percentage.” For a personal system, this is enough to compute daily close, volume, delivery, rolling highs, EMA/SMA inputs, and most of the screeners you listed. citeturn11view6turn11view7turn17view1turn9search0turn9search8

For your pre-market context layer, use Gift Nifty as the primary offshore India cue and keep “SGX Nifty” only as a backward-compatible alias in the UI. Official NSE material says the SGX-traded Nifty derivative contracts moved to NSE IX and were renamed Gift Nifty, with trading access extended to nearly 21 hours and SGX-member order routing into NSE IFSC. Your own class notes also explicitly say Gift Nifty has replaced SGX Nifty. So the system should store one field like `giftNiftyChangePct`, and if you still want the legacy label for habit, render it as “SGX/GIFT” in the UI without creating a second data source. citeturn11view1turn11view0 fileciteturn0file6 fileciteturn0file8

For global risk cues, use futures, not random headline sites. entity["company","CME Group","us derivatives exchange"] is the right benchmark source for E-mini Dow, gold futures, and WTI crude futures. CME identifies E-mini Dow as the `$5 x DJIA` contract with symbol YM, and it clearly documents benchmark gold and WTI crude contracts. Your notes treat Dow futures as a direct cue and gold and crude as inverse risk cues; your current market-context model already converts those into percentage-change inputs and posture bands. That part is correct in spirit. What is not correct yet is the absence of DII in the model, because your notes explicitly say FII negative plus DII positive implies resilience. citeturn5search3turn10search0turn10search1 fileciteturn0file15 fileciteturn0file6 fileciteturn0file8

For intraday data, choose one of two clean paths. If you want direct multi-hour candles, entity["company","Upstox","india broker"] V3 is better because it supports custom candle intervals in minutes and hours, including 1–5 hour windows. If you want broad ecosystem support and strong instrument metadata, entity["company","Zerodha","india broker"] is also solid, but its historical API gives you intervals from minute up to daily, which means you may need to aggregate minute or hourly candles into 4-hour bars yourself. Zerodha also says the instruments list should be fetched once a day, ideally around 08:30, and stored locally. That advice is right; do not hit instruments endpoints repeatedly during screening. citeturn11view5turn11view4turn16search0

The public pages from entity["organization","The Economic Times","india business newspaper"], entity["organization","Moneycontrol","india finance media"], and entity["company","ICICI Direct","india brokerage"] are useful, but only as reference surfaces. The ET RSI-above-80 page is clearly useful for parity checks, but the live page is scoped to Nifty 500 on-page, so it is not a complete market universe by itself. Moneycontrol’s Top Gainers and Volume Shockers pages are also useful references for BTST because they describe momentum and volume-multiple logic, but you can reproduce both internally from your own universe by ranking daily return and comparing current volume to the 5-day average volume. Chartink itself states its prices are sourced from NSE feed and fundamentals from CMOTS, which is fine for research, but in fetched HTML several of your linked pages exposed only titles or short descriptions rather than stable machine-readable expressions, so building your engine around scraping them would be dumb. Use them to validate, not to think. citeturn17view0turn17view2turn17view3turn17view4turn1view0turn1view1

## What the daily cron should do

Your existing cron spec is mostly right: pre-market at 08:30 IST, post-close at 16:30 IST, month-end at the last calendar day around 17:00, plus provider-health checks. That lines up with NSE cash-market timing well enough, because the pre-open session begins at 09:00, normal market opens at 09:15, normal cash market closes at 15:30, and the closing session runs until 16:00. So 08:30 is a reasonable time for context assembly, and 16:30 is a reasonable time for a post-close screener pass. fileciteturn0file13 citeturn19view0turn19view2turn19view3

The pre-market job should do five things, and it should do them deterministically. It should fetch Gift Nifty, E-mini Dow, gold, crude, previous-session FII flow, previous-session DII flow, the holiday/expiry calendar state, and the prior day’s own context snapshot. It should then score each factor as favorable, neutral, hostile, or missing, exactly like your current context model does for GIFT, Dow, gold, crude, and FII, but it must be upgraded to add `diiNetCashCr` and then a derived resilience override when FII is negative and DII is positive. Right now your market-context doc does not model that override even though the notes do. That is a real gap, not a cosmetic issue. fileciteturn0file15 fileciteturn0file13 fileciteturn0file8

The post-close job should compute indicators, run all daily screeners, store explanation traces, and generate intersections. This is where your screener bundles matter. “Swing Daily Check” should run Buying in the Dips, Cross, ABC, and Trend Continuation. “Breakout Radar” should run breakout quality, 52-week highs, and RSI above 80. “Strong Confluence Set” should intersect trend continuation, breakout quality, 13/34, and 4-hour EMA/SuperTrend. If a stock passes multiple bundles, that is a confluence score, not a buy order. Your prompt is explicit that the system should explain matched rules and context, not pretend certainty. fileciteturn0file11 fileciteturn0file18 fileciteturn0file0

The BTST sub-job needs hard gates, not vibes. Your notes say avoid Friday, avoid the day before a holiday, and avoid the day before expiry; the cleaner session summary calls out Tuesday expiry distortion. As of the current official NSE contract specification, key index and single-stock equity-derivative expiries are Tuesday, with the previous trading day used if Tuesday is a holiday. So your BTST gate should be data-driven: `skip if next trading day is holiday` or `skip if tomorrow is expiry for the relevant derivatives regime`. Do not hardcode old Thursday logic. That would be outdated. fileciteturn0file8 fileciteturn0file7 citeturn13view0turn20view0

The month-end job should stay investment-only. Your own docs already define a month-end pipeline and a Month End Investment bundle. Use that to run the monthly BB breakout and heuristic MBB candidate scans on the last trading day after close, compare the latest completed month with the prior completed month, and snapshot candidates separately from swing names. That “last working day / previous to last working day” instruction belongs here; it does not belong inside your daily swing cron. fileciteturn0file11 fileciteturn0file13 fileciteturn0file4

## What your current codebase should change next

Your strategy DSL is already the right backbone. It supports deterministic filters, time-scoped fields like `daily`, `monthly`, and `h4`, canonical version tags, and explainability output. Keep that. Do not replace it with ad hoc if-else blobs. What you should do next is formalize every screener you care about as DSL-backed strategies plus DSL-backed screener views, then persist the condition-by-condition trace so you can answer “why did this stock show up?” without hand-waving. fileciteturn0file17

From the gap audit and roadmap, the next real work is obvious. Tighten validation. Finish `/api/screeners`, `/api/screeners/run`, and the bundle-save workflow. Add the missing internal screeners for sideways support reversal and 20-day channel breakout. Merge worker logic and web-route logic so cron behavior is not duplicated. Add idempotent locks, retries, and force-rerun semantics exactly as the cron spec says. Enforce degraded mode properly when one or more providers fail instead of silently guessing. Then write the tests you claim you want: parser/validator tests, market-context tests, screener intersection tests, month-end gating tests, degraded-mode tests, and Playwright smoke tests. fileciteturn0file9 fileciteturn0file10 fileciteturn0file13 fileciteturn0file16

There is also doc drift in your repo, and you should stop ignoring it. The gap audit says some digest routes and source-reference screens were missing, while the screen matrix marks them complete. That means one of your documents is stale, or the implementation moved faster than the audit did. Either way, prose is no longer trustworthy enough. Route-level Playwright tests and API contract tests should decide what is actually shipped. Until that happens, assume completion claims are provisional. fileciteturn0file9 fileciteturn0file19 fileciteturn0file16

On deployment, keep it boring. Your deployment doc already gives you the minimum viable production shape: Node 20+, PostgreSQL/Supabase, Next.js web app, optional worker, cron endpoints protected by `CRON_SECRET`, and date-scoped lock keys. That is fine. If you want this stable, add Redis or an equivalent lock/cache layer the way your main prompt suggests, because cron idempotency is not something you should fake with wishful thinking. fileciteturn0file14 fileciteturn0file0

## Keep the screener separate from the money rules

Your “add $100 every month,” “if profit is $10 then split 30/20/30/20,” and “A → bitcoin/betting, S → AI tokenization” rules are not screener rules. They are capital-allocation rules. If you shove them into the screener engine, you will corrupt the design. The screener engine should answer: what matches, why it matches, what context is favorable or hostile, and how risky the structure is. The portfolio-policy layer should answer: how much fresh cash goes in monthly, how profit gets distributed after a sale, what goes to low-risk buckets, and what goes to experimental buckets. Those are different systems and should stay different. Your own notes already separate investing, swing trading, and risk management; keep doing that. fileciteturn0file0 fileciteturn0file8

That portfolio layer can still be useful. Use the monthly-investment future-value formula from entity["company","ICICI Direct","india brokerage"] to power a small goal-planning widget, but keep it downstream from the screener. Screening finds candidates. Position sizing then uses the 2% risk rule and quantity formula from your notes. Portfolio policy then decides whether realized profits are recycled, withdrawn, parked in safer assets, or routed to an exploration bucket. If you blur those stages, you will not know whether a result came from a good setup, a bad rule, or a bad capital decision. citeturn17view4 fileciteturn0file8 fileciteturn0file3

The blunt answer is this: you already have enough raw material to build the system, and most of the architecture is already implied by your codebase. What you do not yet have is a disciplined boundary between reference pages and source-of-truth data, between daily context and quarterly ownership, between support-reversal entries and breakout entries, and between screening logic and portfolio policy. Fix those boundaries first. After that, the daily cron, internal screeners, and end-to-end workflow become straightforward. fileciteturn0file0 fileciteturn0file9 fileciteturn0file10